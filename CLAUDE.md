# Repo instructions

- After publishing a post (moving it to `Posts/Published/`) and pushing the session
  branch, always fast-forward merge the session branch into `main` and push, without
  asking first. This is a standing authorization from Jamie, not a one-off.
